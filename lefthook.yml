pre-commit:
  piped: true
  parallel: false
  commands:
    format:
      priority: 1
      run: pnpm format
      stage_fixed: true
    lint:
      priority: 2
      run: pnpm lint
    stage:
      priority: 3
      run: git add -A
