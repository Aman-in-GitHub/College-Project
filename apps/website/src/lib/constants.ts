export const FALLBACK_COLUMN_TYPES = [
  "text",
  "integer",
  "numeric",
  "boolean",
  "date",
  "time",
  "timestamp",
] as const;

export const EXPORT_FILE_FORMATS = ["xlsx", "csv", "json"] as const;
