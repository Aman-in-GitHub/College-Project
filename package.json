{
  "name": "college-project",
  "private": true,
  "workspaces": [
    "apps/**"
  ],
  "scripts": {
    "clean": "docker compose down && for d in .tanstack dist node_modules pnpm-lock.yaml; do find . -name \"$d\" | xargs -r rm -rf; done",
    "dev": "docker compose up -d && pnpm -r --parallel --if-present run dev",
    "format": "oxfmt",
    "lint": "oxlint",
    "update": "pnpm -r update",
    "update:latest": "pnpm -r update --latest"
  },
  "devDependencies": {
    "lefthook": "^2.1.6",
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@10.33.0"
}
